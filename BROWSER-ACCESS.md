# 🌐 Доступ через браузер с Supabase

Теперь BikePark54 можно использовать не только через Telegram Web App, но и через обычный браузер с **полноценной интеграцией с Supabase**!

## 📁 Новые файлы

### API
- **`api/browser-auth.js`** - API endpoint для регистрации, входа и получения списка пользователей

### Страницы входа/регистрации
- **`index-browser.html`** - Стартовая страница для браузера
- **`browser-register.html`** - Регистрация нового пользователя
- **`browser-login.html`** - Вход для существующих пользователей

## 🚀 Как использовать

### Для разработки (локально)

1. Откройте любой из файлов в браузере:
   ```
   file:///путь/к/проекту/site/index-browser.html
   ```

2. Или используйте локальный сервер:
   ```bash
   cd site
   python -m http.server 8000
   ```
   Затем откройте: `http://localhost:8000/index-browser.html`

### Для продакшена

Загрузите на Vercel/Netlify как обычно. Пользователи смогут:
1. Перейти на `https://ваш-домен.vercel.app/index-browser.html`
2. Зарегистрироваться с именем и фамилией
3. Войти в профиль

## 🔑 Система аутентификации с Supabase

### Регистрация через браузер
1. Пользователь вводит **имя**, **фамилию** и **телефон** (опционально)
2. Запрос отправляется на `/api/browser-auth` (action: 'register')
3. Создается запись в таблице `clients` в Supabase:
   - `telegram_id`: `browser_[timestamp]_[random]`
   - `name`: полное имя
   - `phone`: телефон или null
   - `verification_status`: 'approved' (автоматически)
   - `registration_type`: 'browser'
   - `balance`: 0
4. Возвращается токен для последующей аутентификации
5. Автоматический вход в профиль

### Вход через браузер
1. Загружается список всех браузерных пользователей из Supabase
2. Пользователь выбирает свой профиль
3. Генерируется токен для входа
4. Запрос на `/api/browser-auth` (action: 'login') с токеном
5. Проверка токена и загрузка данных из Supabase
6. Сохранение сессии в localStorage

### Выход
- Кнопка "Выйти" в профиле
- Удаляются данные текущей сессии из localStorage
- Данные остаются в Supabase
- Перенаправление на страницу входа

## 🗄️ Интеграция с Supabase

### Таблица `clients`

Браузерные пользователи добавляются в ту же таблицу, что и Telegram пользователи:

```sql
{
  telegram_id: "browser_1730923456789_xk2j5n",
  name: "Иван Петров",
  phone: "+79001234567" или null,
  balance: 0,
  verification_status: "approved",
  registration_type: "browser",
  created_at: "2025-11-06T..."
}
```

### API Endpoints

#### POST `/api/browser-auth`

**Action: register**
```json
{
  "action": "register",
  "firstName": "Иван",
  "lastName": "Петров",
  "phone": "+79001234567"
}
```

Ответ:
```json
{
  "success": true,
  "user": {
    "id": 123,
    "telegram_id": "browser_...",
    "name": "Иван Петров",
    "phone": "+79001234567",
    "balance": 0
  },
  "token": "base64_encoded_token"
}
```

**Action: login**
```json
{
  "action": "login",
  "token": "base64_encoded_token"
}
```

Ответ:
```json
{
  "success": true,
  "user": {
    "id": 123,
    "telegram_id": "browser_...",
    "name": "Иван Петров",
    "phone": "+79001234567",
    "balance": 0,
    "verification_status": "approved"
  }
}
```

**Action: list-users**
```json
{
  "action": "list-users"
}
```

Ответ:
```json
{
  "success": true,
  "users": [
    {
      "id": 123,
      "telegram_id": "browser_...",
      "name": "Иван Петров",
      "phone": "+79001234567",
      "created_at": "2025-11-06T..."
    }
  ]
}
```

## 🔐 Безопасность

### Реализовано:
- ✅ Хранение пользователей в Supabase
- ✅ Токены для аутентификации
- ✅ Service Role Key для серверных операций
- ✅ Автоматическое одобрение браузерных пользователей

### TODO для продакшена:
- [ ] Пароли с хешированием
- [ ] JWT токены с истечением
- [ ] Refresh токены
- [ ] Верификация email/телефона
- [ ] Rate limiting
- [ ] CSRF защита
- [ ] Логирование действий

## 📊 Хранение данных

### localStorage (клиент):

```javascript
// Текущая сессия
isRegistered: "true"
userId: "browser_1730923456789_xk2j5n"
userName: "Иван Петров"
userFirstName: "Иван"
userLastName: "Петров"
userPhone: "+79001234567"
userBalance: "0"
userClientId: "123"
authToken: "base64_encoded_token"
registrationType: "browser"
registrationDate: "2025-11-06T..."
```

### Supabase (сервер):

Все данные хранятся в таблице `clients` и доступны через API.

## 🎨 Дизайн

Все страницы используют существующий `style.css` с фирменными цветами:
- **Основной оранжевый**: `#FF6B35`
- **Вторичный оранжевый**: `#FF8C42`
- **Темный текст**: `#37474F`

## 🔄 Совместимость

`profile.html` теперь работает в обоих режимах:
- ✅ Telegram Web App (старый функционал)
- ✅ Обычный браузер (новый функционал)

Определение режима:
```javascript
if (window.Telegram?.WebApp?.initData) {
  // Telegram режим
} else {
  // Браузер режим
}
```

## 📝 TODO

- [ ] Добавить восстановление пароля
- [ ] Email верификация
- [ ] Социальные сети (Google, VK)
- [ ] 2FA аутентификация
- [ ] Серверное хранилище пользователей
- [ ] Синхронизация между устройствами
